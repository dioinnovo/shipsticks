export interface BrandConfig {
  name: string
  logo: string
  colors: {
    primary: string
    secondary: string
    accent: string
    success: string
  }
  ai: {
    name: string
    persona: string
  }
}